# TODO: REMOVE

function compute_k_chem_arrhenius(A, n, E_a, T)
    nothing
end

function compute_k_chem_park(T, Tv; s=0.5)
    nothing
end

function compute_k_chem_park(xi_vibr, T, Tv; s=0.5)
    # for better wrapping
    nothing
end

function compute_k_diss_treanor_marrone(xi_vibr, T, Tv, 1_div_U)
    nothing
    # return k_diss, k_rec, k_ev_diss, k_ev_rec
end

function compute_k_diss_macheret_friedman(xi_vibr, T, Tv)
    nothing
    # return k_diss, k_rec, k_ev_diss, k_ev_rec
end

function compute_k_exch_zeldovich_aliat(xi_vibr, T, Tv_reagent, Tv_product)
    nothing
end